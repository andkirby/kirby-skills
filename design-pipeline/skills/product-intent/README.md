# product-intent

Upstream UX skill for converting a raw idea, broad requirements, or an existing product into a compact intention/capability contract.

It deliberately stops before information architecture and UI specification.

Typical handoff:

```text
raw idea / requirements
  -> product-intent
  -> information-architect
  -> ux-designer-specifier
```
